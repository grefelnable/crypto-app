import { Routes, Route } from "react-router-dom";
import styled from "styled-components";

function App() {
  return (
    <div>
      <Routes>
        <Route path="/" />
      </Routes>
    </div>
  );
}

export default App;

// CSS
